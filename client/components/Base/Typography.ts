import styled from '@emotion/styled'

const titleProps = {
  fontSize: '1.5rem',
  fontWeight: '300',
}

export const MainTitle = styled.h1({
  ...titleProps,
})

export const Title = styled.h2({
  ...titleProps,
})
